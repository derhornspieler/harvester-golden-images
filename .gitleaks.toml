# Gitleaks configuration
# CA certificates are public trust anchors, not secrets.

[allowlist]
  description = "Allow CA certificate placeholders and documentation examples"
  paths = [
    '''.*\.tfvars\.example$''',
    '''.*\.md$''',
  ]
