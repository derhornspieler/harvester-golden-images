# Terraform
.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl
crash.log

# Sensitive (root tfvars has CA cert; distro tfvars are safe to commit)
terraform.tfvars
kubeconfig-harvester.yaml
*.pem
*.key

# Build artifacts
reports/

# OS
.DS_Store
